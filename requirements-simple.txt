# Healthcare Symptom Checker - Simplified Requirements for Deployment
# This file avoids Rust compilation issues on platforms like Render

# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database - Use SQLite for deployment (no PostgreSQL dependencies)
sqlalchemy==2.0.25

# LLM Integration
google-generativeai==0.3.2
groq==0.4.1

# Basic utilities (avoid pydantic-core which requires Rust)
pydantic==2.5.3
pydantic-settings==2.1.0

# Simple alternatives for vector operations
numpy==1.26.4
scikit-learn==1.3.2

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
tenacity==8.2.3

# Logging
python-json-logger==2.0.7

# Production Server
gunicorn==21.2.0

# Note: ChromaDB and sentence-transformers removed to avoid Rust compilation
# The application will work with simplified vector operations using scikit-learn
